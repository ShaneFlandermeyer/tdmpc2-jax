# CoRL Compact Dense-RHS V3 Decisions

- 2026-05-13: Created the expanded v3 steward plan. The target matrix is 10 environments x 2 regimes x 2 methods x 6 seeds = 240 valid cells. The v3 ledger reuses the 66 finite v2 main rows and excludes the 6 non-finite fish-swim chaos rows.
- 2026-05-13: V3 uses phased autonomous launch gates: first repair fish-swim chaos, then add seeds 23/31/42 for the original six environments, then run acrobot-swingup and walker-run, then run pendulum-swingup and reacher-hard only after their MJX ports and gates pass.
- 2026-05-13: Fish-swim chaos requires an explicit finite-rollout gate at `runs/results/mjx_gates_chaos/fish-swim/mjx_gate.json` before any fish chaos rerun or downstream full-matrix launch. This prevents silently rerunning the known NaN failure mode.
- 2026-05-13: Fish-swim chaos finite gate job `2099` failed with non-finite rewards and observations under chaos perturbations. The steward now treats gates as per-profile launch blockers rather than globally serializing the campaign, so fish-chaos and ungated new ports remain blocked while independent seed-expansion and ready-environment profiles can launch. Launch priority is shifted to seeds `23, 31, 42` first to improve variance on already-supported environments.
- 2026-05-13: Reserved one steward GPU for fish-swim chaos repair while the fish finite-smoke gate is failing. The v3 steward may run diagnostic MJX gate profiles in that reserved lane and limits ordinary matrix jobs to three active GPUs until `fish_chaos_finite_smoke_gate` passes; once it passes, the reserved slot is automatically released back to the full matrix. One regular matrix job may be intentionally cancelled and marked retryable to free the repair slot.
- 2026-05-13: Replaced the long fish-swim chaos diagnostic sweep with tiny one-case probes because job `2106` spent over 20 minutes compiling/running without writing artifacts. The repair queue now runs 1-env, 4-step probes for clean control, actuator-only, observation-noise-only, delay/jitter, wind-only, push-only, push-decay, and full chaos. These probes write to `runs/results/mjx_gates_chaos_diag/` and do not release the fish-chaos full-matrix gate.
- 2026-05-13: Removed fish-swim from the final v3 matrix after tiny diagnostics isolated a step-1 nonfinite state/reward failure to push perturbations (`push_only`, `push_decay`, and `full_chaos` fail; clean, actuator-only, observation-noise-only, delay/jitter, and wind-only pass). Replaced fish-swim with `cup-catch` as a port-gated DMC manipulation/contact environment. The target matrix remains 240 cells, with expected reused cells reduced from 66 to 60 and expected new cells increased from 174 to 180.
- 2026-05-17: Paused the v3 monitoring heartbeat to add MJX DMC task wiring for the remaining port-gated environments (`cup-catch`, `pendulum-swingup`, and `reacher-hard`) and generate their gate artifacts before reopening the final 72 matrix profiles.
- 2026-05-18: Repaired cup-catch chaos semantics after seed-23 chaos runs collapsed to zero. The generic MJX DMC xfrc fallback targeted the free `ball` body for cup-catch, directly perturbing the reward object and making catch dynamics degenerate. The force target is now the controlled `cup` body for cup-catch, and the two invalid seed-23 cup-catch chaos rows are marked retryable for clean rerun under the repaired environment.
- 2026-05-18: Archived the invalid remote cup-catch chaos seed-23 run directories before rerun to prevent Orbax restore from old 500k checkpoints: `outputs/corl_pub_500k_v3/cup-catch/chaos/paper_horizon/s23_invalid_ball_xfrc_20260518T000008Z` and `outputs/corl_pub_500k_v3/cup-catch/chaos/adaptive_rhs/s23_invalid_ball_xfrc_20260518T000008Z`.
